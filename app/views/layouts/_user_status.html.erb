<% if user_signed_in? %>
  <span id="user-name"><%= link_to current_user.name, edit_admin_user_url(current_user)%></span>
  | <%  if can? :manage, Admin::AdminController %>
    <%= link_to "Configuration", '/admin' %> |
  <%end%>
  <%= link_to "Change Log", "/change_log/index" %>
  | <%= link_to "Change Password", "/admin/users/#{current_user.id}/edit?type=pass" %>
  | <%= link_to "Sign Out", '/logout' %>
<% end %>