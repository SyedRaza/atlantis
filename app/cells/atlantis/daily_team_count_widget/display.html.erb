<div id="active_team_chart" style="width: 100%; height: 200px;"></div>

<script type="text/javascript" charset="utf-8">
    $(function () {
        var options = {
            chart: { renderTo: 'active_team_chart' },
            title: { text: 'New VTeams in Current Week' },
            credits : { enabled: false },
            plotOptions:{
                line: {
                    lineWidth : 1
                },
                series:{
                    marker :{
                        enabled: true,
                        radius : 3
                    }
                }
            },
            xAxis: {
                type: 'datetime',
                maxZoom: 48 * 3600 * 1000
            },
            yAxis:{
                title: {
                    text: ''
                },
                min : 0-0.2,
                max : <%= @teams_count.max %>+0.5,
                endOnTick : false,
                startOnTick : false
            },
            legend:{
                enabled : false
            },
            series: [
                {
                    name: 'Team Count',
                    pointStart: Date.parse("<%= @team_dates[0] %>"),
                    pointInterval: 24 * 3600 * 1000 ,
                    data:<%= @teams_count.inspect %>
                }
            ]
        };

        new Highcharts.Chart(options);
    })
            ;
</script> 